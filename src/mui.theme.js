import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0672CB",
    },
    error: {
      main: "#E4424D",
    },
    success: {
      main: "#9BC438",
    },
    warning: {
      main: "#F5CD6F",
    },
  },
});

export default theme;
